import { NextResponse } from 'next/server'

export async function POST() {
  // TODO: Fase 3 — agente gerador de follow-ups
  return NextResponse.json({ message: 'Followups agent — coming in Phase 3' })
}
