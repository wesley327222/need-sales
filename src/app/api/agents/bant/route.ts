import { NextResponse } from 'next/server'

export async function POST() {
  // TODO: Fase 3 — agente BANT (Budget, Authority, Need, Timeline)
  return NextResponse.json({ message: 'BANT agent — coming in Phase 3' })
}
