import { NextResponse } from 'next/server'

export async function POST() {
  // TODO: Fase 3 — agente avaliador (nota1, nota2, nota3)
  return NextResponse.json({ message: 'Evaluator agent — coming in Phase 3' })
}
