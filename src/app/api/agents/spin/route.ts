import { NextResponse } from 'next/server'

export async function POST() {
  // TODO: Fase 3 — agente SPIN Selling (nota4: S, P, I, N)
  return NextResponse.json({ message: 'SPIN agent — coming in Phase 3' })
}
