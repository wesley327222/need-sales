import { NextResponse } from 'next/server'

export async function POST() {
  // TODO: Fase 3 — agente gerador de relatório consolidado
  return NextResponse.json({ message: 'Report agent — coming in Phase 3' })
}
